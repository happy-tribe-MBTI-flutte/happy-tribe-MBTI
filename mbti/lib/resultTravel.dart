import 'package:flutter/material.dart';

class resultTravel extends StatelessWidget {
  const resultTravel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
